<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrossPlatformApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ConnectBtnTest"
                Text="Connect to CMD" 
                Clicked="OnConnectBtnClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="HelloWorldBtn"
                Text="Click me to write input" 
                Clicked="OnWriteInputClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="AdbStartServerBtn"
                Text="Click me to start adb server" 
                Clicked="OnStartServerClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="AdbKillServerBtn"
                Text="Click me to kill adb server" 
                Clicked="OnKillServerClicked"
                HorizontalOptions="Fill" />
            <Frame 
                BorderColor="Gray"
                CornerRadius="10">
                <Label 
                    x:Name="OutputLabel" />
            </Frame>
            <Frame 
                BorderColor="Gray"
                CornerRadius="10">
                <Label 
                    x:Name="ErrorLabel" />
            </Frame>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
